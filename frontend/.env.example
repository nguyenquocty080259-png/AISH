# Copy sang .env rồi chỉnh theo môi trường của bạn (.env đã nằm trong .gitignore).

# Origin của backend cho các URL tĩnh không đi qua apiClient (ảnh đại diện, file /uploads).
# Bỏ trống khi dev với BE chạy ở localhost:8080 — code đã mặc định giá trị đó.
VITE_API_ORIGIN=http://localhost:8080
